version: '3.8'

services:
  # MongoDB 数据库
  mongodb:
    image: mongo:6.0
    container_name: ideal-market-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password123
      MONGO_INITDB_DATABASE: ideal_market
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - ideal-market-network

  # Redis 缓存和任务队列
  redis:
    image: redis:7-alpine
    container_name: ideal-market-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - ideal-market-network

  # 后端API服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ideal-market-backend
    restart: unless-stopped
    depends_on:
      - mongodb
      - redis
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - MONGODB_URI=mongodb://admin:password123@mongodb:27017/ideal_market?authSource=admin
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=your-jwt-secret-key-change-in-production
      - WECHAT_APP_ID=your-wechat-app-id
      - WECHAT_APP_SECRET=your-wechat-app-secret
    volumes:
      - ./backend/uploads:/app/uploads
    networks:
      - ideal-market-network

  # Web前端服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ideal-market-frontend
    restart: unless-stopped
    ports:
      - "8082:80"
    networks:
      - ideal-market-network

  # Nginx 反向代理
  nginx:
    image: nginx:alpine
    container_name: ideal-market-nginx
    restart: unless-stopped
    depends_on:
      - backend
      - frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/conf.d:/etc/nginx/conf.d
    networks:
      - ideal-market-network

volumes:
  mongodb_data:
  redis_data:

networks:
  ideal-market-network:
    driver: bridge